import styled from "styled-components";

export const BackButton = styled.div`
    width: 95%;
    display: flex;
    align-items: center;
    color: var(--primary);
    font-family: "Roboto-Bold";
    font-size: 1rem;
    padding: 10px;
    cursor: pointer;
    overflow: hidden;

    .svg-color {
        width: 25px;
    }
`;

import styled from "styled-components";

export const BackButton = styled.div`
    width: 100%;
    display: flex;
    align-items: center;
    color: var(--primary);
    font-family: "Roboto-Bold";
    font-size: 1rem;
    padding: 10px;
    cursor: pointer;
    overflow: hidden;
    margin-left: 10px;

    .svg-color {
        width: 25px;
    }
`;
